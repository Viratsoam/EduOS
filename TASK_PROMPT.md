# EduOS Task Prompt

Follow `MASTER_PROMPT.md` strictly.

Before implementing:

1. Read the existing files related to the task.
2. Reuse existing utilities, components, DTOs, services, repositories, types, and conventions.
3. Confirm tenant isolation, permission checks, validation, logging, and error handling for backend work.
4. Confirm accessibility, responsiveness, reusable UI, and state boundaries for frontend work.

Rules:

- Do not regenerate existing files.
- Do not rewrite unrelated code.
- Implement only the requested feature.
- Keep the architecture consistent.
- Generate production-ready code.
- Use strict TypeScript.
- Do not use `any`.
- Keep AI features grounded only in approved organization content.
- Add tests according to risk.
- Keep responses concise.

Every response must end with:

- Files Created
- Files Modified
- APIs Added
- Database Changes
- Components Added
- Tests Added
- Testing Instructions
- Next Steps

Do not explain concepts unless explicitly asked.

Wait for the next feature.
