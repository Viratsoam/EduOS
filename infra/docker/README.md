# EduOS Docker Infrastructure

Docker assets for local development and future deployment.

Expected services:

- PostgreSQL
- Redis
- OpenSearch
- MinIO

Start local infrastructure from the repository root with:

```text
docker compose -f infra/docker/docker-compose.yml up
```
